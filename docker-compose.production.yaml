services:
  postgres:
    image: postgres:16
    container_name: postgres-db-prod
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password}
      POSTGRES_DB: ${POSTGRES_DB:-monorepo}
      TZ: "UTC"
    volumes:
      - postgres_data_prod:/var/lib/postgresql/data
      - ./infrastructure/postgres/pg_hba.conf:/etc/postgresql/pg_hba.conf
      - ./infrastructure/postgres/postgresql.conf:/etc/postgresql/postgresql.conf
    command: ["postgres", "-c", "config_file=/etc/postgresql/postgresql.conf"]
    networks:
      - internal_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  migrator:
    build:
      context: .
      dockerfile: apps/backend/Dockerfile
    container_name: migrator-prod
    environment:
      DATABASE_URL: postgres://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-password}@postgres:5432/${POSTGRES_DB:-monorepo}
    depends_on:
      postgres:
        condition: service_healthy
    command: >
      sh -c "
        bun run --filter @manifest/prisma prisma:generate &&
        bun run --filter @manifest/prisma prisma:push
      "
    networks:
      - internal_network

  backend:
    build:
      context: .
      dockerfile: apps/backend/Dockerfile
    container_name: backend-prod
    environment:
      DATABASE_URL: postgres://${POSTGRES_USER:-postgres}:${POSTGRES_PASSWORD:-password}@postgres:5432/${POSTGRES_DB:-monorepo}
      PORT: 3000
    depends_on:
      postgres:
        condition: service_healthy
      migrator:
        condition: service_completed_successfully
    networks:
      - internal_network

  frontend:
    build:
      context: .
      dockerfile: apps/frontend/Dockerfile
      args:
        VITE_API_URL: /trpc
    container_name: frontend-prod
    ports:
      - "8080:80"
    depends_on:
      - backend
    networks:
      - internal_network

volumes:
  postgres_data_prod:
    driver: local

networks:
  internal_network:
    driver: bridge